/**
  {{projectName}}

  Created by Generator
  Copyright (c) 2012 EKGDev. All rights reserved.
  Version: {{version}}
*/

package {{{packagename}}}.logic.utils;

/**
 * 
 * @author Jaume Grane
 * 
 *         Use for async requests
 */
public interface NotifiedHttpRequest {

	/**
	 * Status equals to 200
	 * 
	 * @param response
	 * @param tag
	 */
	void notifySuccess(BetterHttpResponse response, int tag);

	/**
	 * Status distinct to 200
	 * 
	 * @param response
	 * @param tag
	 */
	void notifyError(BetterHttpResponse response, int tag);

	/**
	 * Can not connect to host
	 * 
	 * @param tag
	 */
	void notifyNoConnection(int tag);

	/**
	 * Generic error during connection to host
	 * 
	 * @param e
	 * @param tag
	 */
	void notifyErrorConnecting(Exception e, int tag);

}

