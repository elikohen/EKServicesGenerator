package {{{packagename}}}.logic;

public interface HttpOperation {
	
	/**
	 * Cancels and interrupts if running
	 */
	public void cancel();
	
	/**
	 * Cancels interrupting depending on the parameter.
	 * @param interruptIfRunning
	 */
	public void cancel(boolean interruptIfRunning);

	/**
	* Returns whether this operation is already cancelled.
	*/
	public boolean isCancelled();
}
